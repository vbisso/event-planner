{
  "name": "event-planner",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm install && ls -la node_modules/@types/ && npm list @types/node",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run swagger && ts-node-dev src/index.ts",
    "swagger": "ts-node src/swagger.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.16.0",
    "swagger-ui-express": "^5.0.1",
    "@types/node": "^22.15.21",
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.2",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/mongodb": "^4.0.6",
    "ts-node-dev": "^2.0.0",
    "swagger-autogen": "^2.23.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
